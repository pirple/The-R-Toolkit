## Fun with Vectors
# character
a <- c("apple", "orange", "grapes", "melon", "banana")

#numeric
b <- c(45, 21, 9, 23, 15)

# vector based math
c <- c(3, 5, 8, 17, 29)

# table uses vectors
d <- data.frame(col1 = a,col2 = b,col3 = c)

# SQUARE brackets
b[c(1, 5, 3)]
